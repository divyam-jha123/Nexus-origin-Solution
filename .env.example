# Backend API server
PORT=5001
JWT_SECRET=change_me_in_production
GOOGLE_APPS_SCRIPT_URL=

# SMTP (optional - email dispatch is skipped if unset)
SMTP_USER=
SMTP_PASS=

# Frontend (Vite) - must match the backend PORT above
VITE_API_BASE=http://localhost:5001/api
